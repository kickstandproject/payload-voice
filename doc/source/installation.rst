============
Installation
============

At the command line::

    $ pip install payload-voice

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv payload-voice
    $ pip install payload-voice